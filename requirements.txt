streamlit==1.40.2
plotly==5.24.1
pandas==2.2.3
reportlab==4.2.5
Pillow==11.0.0
requests==2.32.3
chardet<6          # keeps requests from emitting a version-mismatch warning
pytest==8.3.4

# Gemini, for AI explanations and crop-image assessment.
# Imported lazily in services/llm.py and services/vision.py, so the app still
# runs (on engine-written explanations) if this is unavailable.
google-genai>=1.0.0
